"""
Tilt extension for mocks/six-mts-stub/.

Phase 3 (US1) — der Mock läuft in-process im venue-adapter-six
(Property `swisstms.six.simulate-fills=true`). Eine eigene Container-
Komponente kommt in Phase 14, wenn echte QuickFIX-Initiator-Sessions
gegen einen separaten Endpunkt laufen.
"""
# Intentionally a no-op for now.
